# Getting Started

The best source for getting started is the examples - they're designed to be self contained, and don't rely on many helpers beyond shader compilation. Most features of the library should be used in at least one example.

A higher-level overview is below.

## Initialization

The starting point should be calling `create_device()`. 